parameters:
  - name: configuration
    type: object
    default: {}

resources:
  repositories:
    - repository: PipelineCommon
      type: github
      endpoint: wesley-trust
      name: wesley-trust/pipeline-common
      ref: 'refs/heads/feature/var-refactor'
      #ref: 'refs/heads/main'

extends:
  template: templates/main.yml@PipelineCommon
  parameters:
    configuration: ${{ parameters.configuration }}
